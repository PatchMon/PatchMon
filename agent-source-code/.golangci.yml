version: "2"

linters:
  enable:
    # Enabled by default linters
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    # Additional useful linters
    - gosimple
    - misspell
    - revive
    - gosec
